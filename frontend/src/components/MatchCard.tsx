import type { Match } from '../types'
export default function MatchCard({match}:{match:Match}){const date=new Date(match.kickoff);const finished=match.status==='finished';return <div className="panel p-4 md:p-5"><div className="mb-4 flex items-center justify-between text-[10px] uppercase tracking-wider text-slate-500"><span>{match.stage}{match.group_name&&` · ${match.group_name}`}</span><span>{finished?'已结束':date.toLocaleString('zh-CN',{month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit'})}</span></div><div className="grid grid-cols-[1fr_auto_1fr] items-center gap-3"><div className="text-center"><div className="text-3xl">{match.home_flag}</div><div className="mt-2 text-sm font-bold">{match.home_team}</div></div><div className="min-w-[76px] text-center">{finished?<div className="text-3xl font-black tabular-nums">{match.home_score} : {match.away_score}</div>:<><div className="text-xs font-black text-neon">VS</div><div className="mt-1 text-[10px] text-slate-600">{match.venue}</div></>}</div><div className="text-center"><div className="text-3xl">{match.away_flag}</div><div className="mt-2 text-sm font-bold">{match.away_team}</div></div></div></div>}
